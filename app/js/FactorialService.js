var FactorialService = (function() {

    function calculate(number) {
        if (number == 0 || number == 1) return 1;
        return 120;
    }

    return {
        "calculate" : calculate
    }

})();